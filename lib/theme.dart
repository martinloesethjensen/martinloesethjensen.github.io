import 'package:jaspr/dom.dart';

// Personal terminal theme — not GitHub's palette
const termBg = Color('#090d12');        // deeper, cooler than #0d1117
const termCard = Color('#101620');       // blue-tinted dark surface
const termTitlebar = Color('#18243a');   // distinctly blue-shifted
const termBorder = Color('#253050');     // saturated blue-gray

// Mobile developer accent identity
const termGreen = Color('#3ddc84');      // Android Studio green
const termGreenBright = Color('#56f09b');
const termCyan = Color('#54c5f8');       // Flutter's primary light blue
const termText = Color('#dde5f0');       // cool white
const termSubText = Color('#a8b8c8');    // blue-tinted mid
const termMuted = Color('#607890');      // slate
const termComment = Color('#6a8098');    // blue-gray, distinct from GitHub's neutral gray
const termYellow = Color('#f0a030');     // warmer amber

// Aliases kept for compatibility
const primaryColor = termGreen;
const primaryDarkColor = termGreenBright;
const darkColor = termText;
const textColor = termSubText;
const mutedColor = termMuted;
const lightBg = termBg;
const cardBg = termCard;
const borderColor = termBorder;
const tagBg = Color('#192540');  // blue-tinted dark tag surface
const tagText = termCyan;
const heroBg = termBg;
const heroText = termText;
